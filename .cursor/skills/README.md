# Listae agent skills

Project skills (auto-discovered). English. Specialize global templates from `~/.agents/skills/`.

| Skill | Role |
|-------|------|
| `listae-theme-engine` | Profile HTML/CSS, Google Fonts allowlist, diagnostics |
| `listae-media-model` | Work/ListEntry domain |
| `listae-catalog-providers` | TMDB / Open Library / manual + cache port |
| `listae-docs-sync` | ADR + context maintenance |

Global templates (installed for this user): `user-profile-theme-engine`, `media-tracking-domain`, `search-cache-store`, `living-project-docs`, plus `frontend-design` and `find-skills`.
